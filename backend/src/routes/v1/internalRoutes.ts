import { Router } from 'express';

const router = Router();

// Internal authenticated routes will be added here as features are implemented
// Example: router.use('/order', orderRoutes);

export default router;
